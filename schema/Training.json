{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "Training.json",
  "title": "Training",
  "type": "object",
  "required": ["status"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["running", "failed", "success"]
    }
  }
}
