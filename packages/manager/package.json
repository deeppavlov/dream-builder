{
  "name": "manager",
  "version": "1.0.0",
  "scripts": {
    "start": "poetry run uvicorn manager.main:app --port 5555 --reload",
    "install": "poetry install"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
